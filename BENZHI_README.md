# MiniEnvoy 评测说明

本项目是基于Go语言的MiniEnvoy，旨在解决MiniEnvoy相关的工程问题，使用了Go，功能有以 Go 直接调用 Linux Epoll、使用预分配、可复用的无锁环形字节缓冲；`Recv、定义自研二进制应用层协议并完成反向代理、实现轮询或加权路由、后端微服务连接池。

Go 模块位于 仓库根目录。评测入口：在该目录执行 `go test ./...`。
